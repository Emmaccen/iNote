<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    tools:context=".HomePage">
<TextView
    android:layout_margin="4dp"
    android:drawableStart="@drawable/profile_password_icon_24dp"
    android:drawablePadding="10dp"
    android:textAppearance="@android:style/TextAppearance.Material.Title"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:text="Input Password"/>

    <EditText
        android:maxLength="4"
        android:gravity="center"
        android:layout_margin="4dp"
        android:id="@+id/private_note_password_edit_text"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:ems="10"
        android:inputType="numberPassword" />

    <TextView
        android:onClick="resetPasswordButton"
        android:id="@+id/private_note_reset_password"
        android:drawablePadding="4dp"
        android:drawableEnd="@drawable/private_note_reset_password_icon_24dp"
        android:layout_margin="8dp"
        android:textSize="16sp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Reset Password"
        android:gravity="end|bottom"
        />
</LinearLayout>
